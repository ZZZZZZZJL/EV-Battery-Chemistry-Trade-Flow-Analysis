# Codex Actions

- `validate_repo.py`: guard the formal repo layout
- `inspect_bundle.py <bundle_root>`: inspect a runtime bundle manifest
- `smoke_test_app.py`: run minimal app smoke checks
- `dump_route_map.py`: print the current route map
