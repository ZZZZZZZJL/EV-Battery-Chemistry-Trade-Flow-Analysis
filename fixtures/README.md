# Public Fixtures

This directory is reserved for minimal public samples only.

- Keep only tiny contract fixtures or sample bundle metadata here.
- Do not restore private production workbooks, raw imports, or full runtime releases.
- Private operational data now lives under `production_data_processing/` at the workspace level.
