services:
  - type: web
    name: trade-flow-opt
    runtime: python
    plan: starter
    buildCommand: pip install -e .
    startCommand: uvicorn --app-dir . apps.web.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    autoDeploy: true
