services:
  - type: web
    name: ev-battery-chemistry-trade-flow-analysis
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && plotly_get_chrome -y
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 300 app:app
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
